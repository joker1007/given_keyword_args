require 'spec_helper'

describe GivenKeywordArgs do
  it 'has a version number' do
    expect(GivenKeywordArgs::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
